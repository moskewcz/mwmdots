[user]
        name = Matthew Moskewicz
        email = mmoskewicz@tesla.com
[core]
        editor = emacs_nw
[alias]
        start = !git init && git commit --allow-empty -m \"empty initial commit\"
        uberlog = log --graph --all --decorate
        lol = log --graph --decorate --pretty=oneline --abbrev-commit
        lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
[push]
        default = current # note: remove or move to simple when we drop git 1.7.mumble support
